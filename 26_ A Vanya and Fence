#include<iostream>
using namespace std;
int main()
{
	int n, height, personLength,count=0;
	cin >> n >> height;
	for (int i = 0; i < n; i++)
	{
		cin >> personLength;
		if (personLength > height)
			count += 2;
		else
			count += 1;
	}
	cout << count;

	return 0;
}
