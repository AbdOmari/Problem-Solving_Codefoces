#include<iostream>
using namespace std;
int main()
{
	int num , ans = 0;
	cin >> num;
	string str;
	cin >> str;
	for (int i = 0; i < num; i++)
	{
		if (str[i] == str[i + 1])
			ans++;

	}
	cout << ans;
}
